// Tipo compartido para la clasificación del semáforo (verde/amarillo/rojo).
// Coincide con el enum Semaphore de Rust serializado como lowercase.

export type Semaphore = 'verde' | 'amarillo' | 'rojo';

- 2026-08-24 — feature 29 onboarding-auto-gate-startup: gate automático de onboarding al arrancar. Backend: nuevo command `obtener_perfil_activo_con_onboarding` devuelve `{ snapshot, onboarding_status }` del perfil activo en una llamada (evita race conditions); 4 tests cargo TDD (NotStarted, InProgress, Completed, legacy Completed). Frontend: SnapshotProvider usa snapshotPort.obtenerPerfilActivoConOnboarding(), estado extendido con variante `onboarding`; App.tsx renderiza OnboardingWizard full-screen si NotStarted/InProgress, AppShell si Completed; al completar wizard → recargar snapshot → transición a AppShell. Reutiliza OnboardingWizard/useOnboarding existente; botón Saltar funciona igual que en Ajustes. TDD node:test 17 tests (rojo→verde). Verificación completa: cargo test 309/309, pnpm test 584/584, ./init.sh verde. Arquitectura hexagonal: grep tauri domain/application = 0, grep invoke solo en adapters, ≤100 líneas/archivo. Artefactos: specs/29_onboarding-auto-gate-startup/, progress/impl_29_onboarding-auto-gate-startup.md.
